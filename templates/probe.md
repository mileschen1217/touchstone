# Template probe (spike placeholder)

This file exists at `<plugin-root>/templates/probe.md` to verify whether a SKILL.md inside `skills/spike/` can reach it via `../../templates/probe.md`.

If you see this line quoted in the spike's output, the init skill will be able to copy bundled templates to adopting projects using plugin-root-relative paths.

MARKER: PLUGIN_ROOT_TEMPLATE_VISIBLE_2026_05_17
