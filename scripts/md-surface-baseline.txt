# md-surface byte baseline — enforced by scripts/check-md-surface-budget.sh.
# Surface = skills/**/*.md + agents/**/*.md (content bytes, all files summed).
# Ratchet rule: raise ONLY in the same PR as the growth it funds; append the
# reason as a dated comment line here. Lowering after a slimming PR is the
# ratchet doing its job — record it the same way.
# 2026-07-06: initial baseline = surface total at main (md-essence-rewrite net ratchet).
# 276114
# 2026-07-06: ratcheted down to the md-essence-rewrite PR's final total (B1-B6
# rewrites - new vendored agents/base fragment - review-fix restorations).
# 273616
# 2026-07-06: ratcheted down — codex-tdd agent retired + security/database
# specialist fan-out retired (user ruling; roster policy in code-review README).
# 263279
# 2026-07-09: raised for interview-mechanics Phase 1 — assay presentation
# mechanics + crucible PRD+seams light check are new instruction content on
# the shipped surface (funded deletions insufficient; spec Error Handling
# row 5: one-line reason presented for the human's baseline ruling at accept).
# 266765
# (adjusted +137 for the final-review fix wave: light-check fence instruction)
# 266902
# 2026-07-09: raised +2473 for interview-mechanics Phase 2 suite-consistency
# audit — INV-2 negative-routing clauses (10 skills/agents) + INV-3 live-user
# declarations (5 skills) that did not previously exist. The "information
# re-placement, net≈0" assumption (Phase 1 assay flip-trigger) was falsified:
# the negative routing was genuinely absent and had to be added, not moved.
# Human ruled baseline-raise over re-slim (re-slim would delete required
# routing signal). Per-file deltas + disposition in the epic's Phase 2 record.
# 269375
# 2026-07-10: raised +1377 for the Catch-attribution sweep pre-filter (Cut 2)
# + agents-write-to-files return path (Cut 4) — new close-procedure content in
# the lazy-loaded close-and-doc-reckoning.md reference (Step 1b prefilter +
# Step 2 survivor-chunk + file-write dispatch). Genuinely-new mechanism, not
# re-placeable; its RUNTIME effect is a large NET token SAVING (the pre-filter
# stops the paid L1 haiku pass from classifying ~80% blank/tool-result records
# per close), so the doc cost is dwarfed by the runtime win it documents.
# 270752
# 2026-07-10 (same PR extend): the two cold-agent sweep prompts (L1 classify,
# L2 synth) were relocated out of close-and-doc-reckoning.md into their own
# single-home fragments (references/sweep-l{1,2}-*.md) and injected by path —
# the close reference dropped 400→330 lines; net surface slimmed 107 bytes as
# the "MUST inline … verbatim" scaffolding + fences collapsed to a path pointer.
270645
