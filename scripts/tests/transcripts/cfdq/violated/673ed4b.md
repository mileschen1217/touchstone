# Live honor-check transcript — AC-5 (violated / SSP-shape)

**Provenance:** fixture `scripts/tests/fixtures/cfdq/violated/` · arm = feedback (deliverable review) · commit `673ed4b` · run 2026-06-29 · producer = orchestrator `Agent()` dispatch of a cold design-soundness reviewer with the inject fragment.

**Asserted behaviour (AC-5):** a finding naming the violated `## Architecture` commitment.

## Reviewer transcript (verbatim verdict)

Spec commitment: "SspSession SHALL be deep — SHALL encapsulate the full session lifecycle so callers invoke one method; SHALL NOT leak its orchestration sequence to callers."

- Commitment sub-1 (single-method encapsulation) — **Violated.** `SspSession` exposes 10 fine-grained mutators (`connect`, `authenticate`, `begin_transaction`, `execute_command`, `disconnect`, …) with no unified entry point.
- Commitment sub-2 (SHALL NOT leak orchestration) — **Violated.** The handler must manually sequence all 10 calls in protocol order; the sequence is imposed on every caller. The `status` field is same-name-two-meaning (callers read it to know which calls are valid).

**Verdict: FINDING RAISED** — both sub-commitments violated.

## AC outcome

AC-5 **witnessed** — the lens raised a finding naming the violated commitment against the SSP-shaped shallow module, without an external audit. The epic's acceptance bar is met.
