# Live honor-check transcript — AC-12 (multi-commitment granularity)

**Provenance:** fixture `scripts/tests/fixtures/cfdq/multi/` · arm = feedback (deliverable review) · commit `673ed4b` · run 2026-06-29 · producer = orchestrator `Agent()` dispatch of a cold design-soundness reviewer with the inject fragment.

**Asserted behaviour (AC-12):** one finding per violated commitment; none for honored ones.

## Reviewer transcript (verbatim verdict)

Spec carries two commitments:

- Commitment 1 (CacheLayer SHALL be deep — hide TTL/eviction/policy/arithmetic) — **NO FINDING** (honored; interface is `get`/`set`/`delete` only, internals hidden).
- Commitment 2 (RateLimiter SHALL encapsulate check-and-record atomically in a single `check(user_id)`) — **FINDING** (the code exposes `check_limit` + `record_attempt` as two methods; sequencing shifted to callers; a caller omitting `record_attempt` silently bypasses the limit).

**Verdict:** one finding (Commitment 2), none for Commitment 1 — commitment-granularity.

## AC outcome

AC-12 **witnessed** — the lens raised exactly one finding for the violated commitment and none for the honored one (correct granularity).
