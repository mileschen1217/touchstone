# Written by /m-workflow:init (dogfooded at E13 bootstrap). Hand-editable.
# This file is committed — anyone who clones the m-workflow repo inherits the adoption.

# Required
schema_version: 1
created_by_plugin_version: 0.1.8
specs_dir: .swarm/specs
adr_dir: .swarm/docs/adr
epics_dir: .swarm/epics
plans_dir: .swarm/plans
archive_specs_dir: .swarm/archive/specs

# Optional
adopted_disciplines: [source-as-truth]
