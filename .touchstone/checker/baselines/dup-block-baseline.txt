# dup-block baseline — pre-existing duplication ratchet (quality-spine P1, 2026-07-17 v2).
# One fingerprint per line = the FULL normalized word run (no prefix aliasing:
# an extended/shortened/edited copy fingerprints differently and still blocks);
# everything after "#" is a comment. A run NOT listed here blocks the commit.
# Populate only through calibration review (verdicts + rates:
# .touchstone/epics/quality-spine/calibration.md). Regenerate candidates with
# DUP_BLOCK_FP=1 .touchstone/checker/pre-commit/check-dup-block.sh
#
# ── (P3 Batch 2 cross-provider slim) codex agent trio + cross-provider composite
#    pair clusters RECALIBRATED OUT: `codex-adversarial-reviewer` /
#    `cross-provider-architect` / `architect` retired and merged into the single
#    `cross-provider-reviewer` composite + 2 arms — the duplicated runs those
#    fingerprints exempted no longer exist (single home each).
# ── inject-instruction lines — "Read and inject verbatim …/_shared/inject/…" at
#    each consumer is the pointer pattern itself. Verdict: exempt.
read and inject verbatim claude plugin root skills shared inject design soundness honor check md into the reviewer
read claude plugin root skills shared inject bridge content gate md and
skills shared inject bridge content gate md skills shared inject standing vs transient bridge md
load vocabulary read claude plugin root skills shared config resolver md with the read tool and follow it exactly
load vocabulary read claude plugin root skills shared config resolver md with the read tool and follow it exactly if source as truth is in bundle disciplines
load vocabulary read claude plugin root skills shared config resolver md with the read tool and follow it exactly if source as truth is in bundle disciplines read claude plugin root skills shared inject bridge content gate md and
read claude plugin root skills shared config resolver md with the read tool and follow it exactly if source as truth is in bundle disciplines
read claude plugin root skills shared config resolver md with the read tool and follow it exactly
# ── parsed contract strings / cold-prompt schema enums. Verdict: exempt
#    (incompressible: sentinel + schema text a consumer parses byte-for-byte).
the sentinel stage review summary critical n high n degraded true false
caught by locus should have locus gap class missing ac false green no gate
# ── (P2 REQ-14/AC-36) U-a evidence-honesty dual-home RESOLVED: the shared core
#    now lives in the single-home fragment skills/_shared/inject/ac-coverage-criteria.md
#    (sibling of ac-coverage-honesty-principle), load-and-injected at code-review batch
#    and epic-close; the 4 U-a exempt fingerprints below are deleted (no restatement
#    remains to exempt).
# ── inject fragments sharing their loading-contract header sentence — each
#    fragment must be self-contained when injected. Verdict: exempt.
a consumer loads this file and follows it carrying only its own delta
# ── (P2 REQ-13/AC-31) U-b crucible-restates-assay-handoff RESOLVED: crucible's body
#    now routes to assay/design-spec by pointer + its own delta (Step 2 no longer
#    restates the consensus handoff; Step 3 points to the confirmed-facts-source
#    fragment rather than restating "an implementation of the ... contract"). The 2
#    U-b fingerprints are deleted (no body restatement remains to exempt). crucible's
#    frontmatter description keeps its one-line handoff summary — a description IS the
#    routing signal, single occurrence, not a body restatement.
# ── template family sharing field rules (design-spec template × epic-index
#    template) — named worked example of an F5 cluster in the authoring template.
#    Verdict: true positive, deferred (shipped surface; P1 invariant forbids fix).
will not take even if related a route not a category up to three sub bullets total flat list never nested
# ── README orientation boilerplate (code-review × design-review READMEs).
#    Verdict: true positive (minor), deferred (shipped surface).
review maintainer notes orientation for maintainers the executable procedure lives in skill md
# ── form-economy size figures restated in insight/SKILL.md vs the authoring
#    template (single home). Verdict: true positive, deferred (shipped surface;
#    P1 invariant forbids fix — P2/P3 essence-rewrite candidate).
guideline 200 lines 2 5k tokens per skill body hard cap 500
# ── severity-tiered stopping-rule pointer lines (P2 REQ-1/AC-41) — the matched
#    text IS the pointer sentence to the single home
#    (skills/_shared/inject/severity-tiered-stopping-rule.md) at each of the 5
#    wiring sites; site bodies carry only their own delta, never the rule body.
#    Verdict: exempt (pointer, not restatement) — same class as the
#    inject-instruction cluster above. Calibration: calibration.md § U-stop.
the severity tiered stopping rule claude plugin root skills shared inject severity tiered stopping rule md single home
read claude plugin root skills shared inject severity tiered stopping rule md and apply it as this