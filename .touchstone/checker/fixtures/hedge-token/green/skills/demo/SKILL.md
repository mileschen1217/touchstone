# demo skill (green) — unconditional imperatives and named-trade-off steering

Run the linter first. Add a regression test for every fix. When speed matters
more than coverage, run the smoke suite and record the gap (trade-off named).

```
Typically hedges inside fenced blocks are contract strings and stay silent.
```

Words like reconsidered or unusually must not match on substring.
