# Fixture project instructions (green)

Dispatch rules live in the demo skill; point at it instead of restating it.

```
Every reviewer must confirm that the dispatch prompt names the goal, the
acceptance condition, and the report format before any subagent run starts.
```

The fenced copy above is a parsed contract string and must stay silent.
