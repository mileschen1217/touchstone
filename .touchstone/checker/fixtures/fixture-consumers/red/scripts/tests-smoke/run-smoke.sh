#!/usr/bin/env bash
# fixture — names fixtures/used so check-fixture-consumers.sh's rule (a) hits.
# the sibling directory this fixture also holds is deliberately not named
# by any script (that is the defect this red fixture exercises).
echo "fixtures/used"
