# Fixture spec (green, legacy flat-AC form — no Requirement headings)

## Acceptance Criteria

### AC-1 — legacy flat spec stays silent

Given a legacy spec
When the checker runs
Then no hit is produced
