## Section A

This is the arm-destined section content for the split lens test fixture, used to compute
the shared per-arm section byte bill across both the cc and codex arms.

## Section B (host)

This is the host-only section. Its bytes must never appear in either arm's dispatched
context bill — only the host session that dispatches this lens reads it.
