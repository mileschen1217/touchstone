---
name: codex-reviewer
---
# codex-reviewer fixture agent

This codex arm placeholder file is written to be longer than the cc arm's placeholder file
so that max_arm_load_tokens (the larger single-arm figure) is provably distinct from the
summed two-arm arm_load_tokens figure, giving the self-test something non-degenerate to
assert about the split-lens byte bill.
